# After the first omg.dev deploy, use its URL for Expo Go and native builds.
# The deployed web app uses its own origin and does not need this value.
EXPO_PUBLIC_OMG_API_URL=https://your-app.apps.omg.dev
